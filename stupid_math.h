#ifndef "_STUPIDMATHH_" //se non è definito il simbolo mathh lo definisco, altrimenti non faccio niente
#define _STUPIDMATHHH_  //il .h serve per definire che il punto h non va compilato. quando uso una funzione appartenente alla libreria, il .h indica che il simbolo esiste
// i due <> in una libreria servono al compilatore per capire che si tratta di una libreria standard che non gli fornisco

float mean(int a, int b);

#endif